# Top-level dependencies for SurplusAS API v2.0.
# Run: python -m venv .venv && .venv\Scripts\pip install -r requirements.txt

# Agent framework
google-adk>=2.0.0b1,<3

# Web server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Config
python-dotenv>=1.0.0

# Cloud SQL access (Phase 3b)
cloud-sql-python-connector[asyncpg]>=1.18
asyncpg>=0.30

# A2A peer calls (Phase 4c)
httpx>=0.27

# Test runner (Phase 6 smoke tests). Not installed in the Cloud Run image.
# Install locally: `.venv\Scripts\pip install pytest`
# pytest>=8
